package org.example;

public abstract class Hero extends Character {
    public Hero(int minHealth, int maxHealth, int minDamage, int maxDamage) {
        super(minHealth,maxHealth,minDamage, maxDamage);
    }
}