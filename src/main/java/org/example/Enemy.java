package org.example;

public class Enemy extends Character {
    public Enemy(int minHealth, int maxHealth, int minDamage, int maxDamage) {
        super(minHealth,maxHealth,minDamage, maxDamage);
    }
}
